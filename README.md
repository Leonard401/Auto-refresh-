# Auto-refresh-
Auto Refresh reloads a page, refreshes a single element, or auto-clicks an element — all on a schedule you set.  • Full page mode reloads the entire tab at a fixed interval. • Element refresh mode re-fetches the page in the background and swaps in just the piece you pick, without a visible reload 
